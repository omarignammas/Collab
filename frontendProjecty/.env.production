# Used automatically by `vite build` (and therefore `tauri build`) — the
# packaged desktop app talks to the real hosted backend instead of a local
# dev server, since a distributed .dmg can't assume one is running.
VITE_API_URL=https://projectii-sk6s.onrender.com
